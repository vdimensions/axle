using System;


namespace Axle.Extensions.Collections.Queue.Fluent
{
    public static class QueueExtensions
    {
        //public static TQueue FluentEnqueue<TQueue, T>(this TQueue queue, T item) where TQueue: class, IQueue<T>
        //{
        //    if (queue == null)
        //    {
        //        throw new ArgumentNullException("queue");
        //    }
        //    queue.Enqueue(item);
        //    return queue;
        //}
    }
}